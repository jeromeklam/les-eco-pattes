import React, { Component } from 'react';

export default class LoadError extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="layout-load-error">
        Component content: layout/LoadError
      </div>
    );
  }
}
