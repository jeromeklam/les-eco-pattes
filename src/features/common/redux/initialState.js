const initialState = {
  geoOn: false,
  geoCoord: {},
  locale: 'fr',
  online: true,
  syncing: false,
  syncEnabled: true,
  socket: null,
  socketEnabled: false,
  rightPanelOpened: false,
  filtersCols: null,
  filters: null,
  sort: null,
  onFilter: null,
  panel: 'none',
};

export default initialState;
